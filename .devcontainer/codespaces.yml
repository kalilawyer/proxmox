services:
  proxmox:
    hostname: pve
    image: dockurr/proxmox
    container_name: proxmox
    environment:
      USERNAME: "root"
      PASSWORD: "root"
    ports:
      - 8006:8006
    privileged: true
    restart: on-failure
    stop_grace_period: 1m
